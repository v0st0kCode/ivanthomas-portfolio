// Isolated test route for the ASCII/Three.js effect Ivan wants to evaluate
// before considering it for the real ProjectCard backgrounds. Not linked from
// any nav — reachable only by visiting /experiments/ascii directly.
import { EffectScene } from '../../components/effect-scene';

const ExperimentsAscii = () => {
  return <EffectScene />;
};

export default ExperimentsAscii;
